# 工程生成提示词

本目录保存可复用的工程生成契约和已填写参数的实例化版本（不代表生成工程已通过完整验收）。提示词描述的不只是功能范围，也包括安全、测试、部署、跨平台启动和验收要求。

## 文件职责

- `rebuild-rbac-system-v1.md`：权威通用模板。新增或修正规则时优先修改此文件。
- `rebuild-rbac_sys-system-v1.md`：以 `rbac_sys` 参数实例化的参考版本，用于生成当前工程和核对模板展开结果。

两份文件当前均为 v1.21。实例化文件不能反向成为另一套独立规范；通用要求发生变化时，应同步更新模板、实例和版本记录。

## 使用流程

1. 复制通用模板，并完整填写 `template_inputs`，不得猜测工程名、namespace、hostname 或端口。
2. 仅替换已定义的模板参数值，保留参数名称；实例文件只保留一个完整输入块，同时保留模板规定的派生关系和九端口登记。
3. 使用 Context7 核对会随版本变化的框架和工具用法；不可用时记录原因并改查官方文档。
4. 检查生成物中没有残留占位符、示例工程名、未登记端口或跨工程资源名。
5. 同步更新变更记录，并验证 Markdown 代码围栏、UTF-8 编码和安全约束。

提示词、README 和示例配置只能记录凭据变量名、来源与安全取值方法，禁止写入真实密码、token、私钥或完整含凭据连接串。

v1.14 修正临时文件权限与秘密传递，明确测试隔离、根 workspace 命令、代理信任、容器重建/健康检查和分阶段验证证据。官方资料及 Context7 核对范围见两份文档第 18 节。

修改后必须按输入块展开通用模板，与实例全文比较（仅标题和实例说明允许不同），同时检查版本、代码围栏和新增测试编号。实现状态与提示词版本分别维护；已有工程的未实现项不得通过降低模板要求消除。

## 容器生成的确定性

v1.15 第 14 节固定服务用途、场景、数量、profile 和清理边界：默认完整开发 7 个，按需加测试数据库和 RedisInsight 后为 9 个；完整 E2E 有独立测试服务，单机生产另有明确基线。九个端口输入不代表九个默认常驻容器。生成工程必须交付 `docs/container-topology.md`、`deploy/service-topology.json`、对应启动入口及 DEP-021～023 验收，禁止自行试建额外长期服务。

这是后续生成/整改的目标契约，不表示当前工程已经实现按需启动或独立 E2E 拓扑。修改提示词不自动改变已有 Compose 配置或运行中的容器；已有实现差距继续由 `docs/implementation-status.md` 跟踪。

## 首次启动的本机证书信任

v1.16 第 14 节新增本机 CA 信任与 Certificate Error 应对，第 15 节启动顺序同步引用。生成工程必须显式完成 CA 身份核对、宿主机信任安装、证书部署及实际浏览器验证，并提供跨平台排障/撤销说明；仅有证书文件、健康容器或通过容器 E2E 均不代表本机浏览器就绪。DEP-024～025 覆盖缺失信任、安装失败、身份不匹配及轮换。此修订只更新生成契约，不表示已经修改当前机器的信任库。

## 模板独立使用与最小提交

v1.17 修正测试配置归属、生产内部服务边界及完整后端测试准备流程。使用通用模板只需提供完整输入块；实例文档已内嵌全部参数，不依赖仓库根目录的 `project-identity.json`。文中要求的实现状态、toolchain、部署文件和验证报告均为生成工程的交付物，不是使用模板前必须存在的文件；历史实例经验需在新环境重新验证。

最小模板提交包含本目录三份 Markdown 和 `tech-stack-baseline-2026-09-08.json` 版本依据；同时保存仓库忽略规则时加根 `.gitignore`，共五个文件。工程源码、部署实现、lockfile 和实例运行报告另行提交。模板与实例一致性检查应直接读取实例的唯一输入块，不依赖未提交的工程文件。此模板版本不代表已有工程完成了新要求。

## 公共契约与未来 AI 边界

v1.18 统一 `row_version`、临时密码来源、输入约束、公共响应模型、逐接口成功响应和错误码目录，固定离线原名恢复确认短语，并补充 API-008～011、BKP-016 验收。第 9 节保留多用户 AI 与长任务设计，使用通用任务示例，明确去重授权范围、任务访问与取消边界；本阶段只记录 ADR，不新增任务表、通用队列、模型调用或 worker 空壳。

Podman 最低版本与实际支持矩阵分开记录，v1.18 曾保留 Redis 8.2.x 安全补丁基线（v1.20 已取消该选型限制）；新主版本发布不等于模板自动升级或已验证兼容。修改后的提示词仍须按实例唯一输入块展开比较；文档静态检查不能计作生成工程的运行验收。Context7 核对来源及本次验证范围见第 18 节。

## 移动端支持范围

v1.19 明确移动端支持为响应式网页：iOS/Android 浏览器与 iPad 复用同一 React SPA、路由与组件，不引入 PWA、Capacitor、React Native 或独立 mobile 代码库，不新增框架或组件库。第 11 节固定断点、viewport meta、WCAG 2.2 SC 2.5.8 触控目标尺寸与窄屏表格降级为卡片/堆叠布局的规则；第 13 节新增 `FE-017`～`FE-019`，用 Playwright 官方 `devices` 注册表在桌面 WebKit/Chromium 引擎中模拟手机/平板/Android 设备。Context7 在本次修订时不可用，已改查 Tailwind CSS、WCAG 2.2 与 Playwright 官方文档，链接见第 18 节；这些验收仍需生成工程实际执行，不代表已在真实物理设备验证。

## 最新稳定版与移动端验收修正

v1.20 曾规定在实施时选择版本；该时间基准已由 v1.21 纠正为 2026-09-08 固定快照。原规则要求所有工具、运行时、直接依赖与镜像采用官方最新正式稳定版，不以本机安装情况或旧主版本上限选型；排除预发布，Node.js Current 与 LTS 分开说明。核对发布元数据和兼容性后记录精确版本、来源、日期，锁定依赖及镜像；CI 不自动追新，真实冲突须记录并明确决定版本例外。v1.20 未生成精确清单；v1.21 已补齐。

移动端 lg 以下统一 Drawer，md 以下卡片、md 以上表格；补齐 768–1023px 行为、44px 独立控件点击区、软键盘、安全区域、动态视口和浏览器最低版本矩阵。FE-017～FE-020 区分设备模拟与 iOS/Android 真机验收，CFG-006 校验版本选型及锁定。真机经受信 staging HTTPS 访问；未执行项不可计为通过。

v1.20 尝试时 Context7 工具未暴露且直接远程 MCP 请求连接失败（v1.21 已重试成功），已按契约回退官方文档（来源见第 18 节）；仅完成文档静态验证，未实施或测试前端应用。

## 当前版本快照（v1.21）

版本固定为现在核对的 **2026-09-08**，以后生成仍沿用本快照，不延后到实施当天选版。包括 Python 3.14.7、Node.js 26.8.1（Current）、npm 12.0.2、React 19.2.8、TypeScript 7.0.2、Vite 8.2.2、React Router 8.3.1、Tailwind CSS 4.3.3、FastAPI 0.141.1、PostgreSQL 18.6、Redis Open Source 8.10.1、Playwright 1.63.0、Podman 6.1.1。完整清单与官方来源内嵌两份提示词第 2 节，并附 [机器可读版本依据](tech-stack-baseline-2026-09-08.json)。直接依赖使用精确版本，未来升级显式修订快照。

本次 Context7 远程 MCP 的 resolve-library-id/query-docs 已成功核对 Tailwind、Playwright、Vite；版本号另以官方发布元数据核实。发现 openapi-typescript 7.13.0 的 TypeScript ^5.x peer 与当前 TypeScript 7.0.2 冲突，保留当前版本并显式记录未解决；没有通过强制安装或降级绕过。仅完成发布元数据与文档静态核对，未宣称整套依赖已安装、镜像 digest 已核实或 UI 已通过测试。
